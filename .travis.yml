language: php

php:
  - 5.3
  - 5.4
  - 5.5
  - 5.6
  - 7

install:
  - composer install --prefer-dist --optimize-autoloader

before_script:
  - php --version

script:
  - php ./vendor/bin/phpunit -c phpunit.xml

notifications:
  email: false

matrix:
  fast_finish: true
  allow_failures:
    - php: 7
